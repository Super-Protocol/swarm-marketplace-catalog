# RAG Agent

A retrieval-augmented agent that answers questions over connected datasets. Present in this catalogue mainly as a worked example of data slots: a required multi-dataset `corpus` slot constrained to datasets that have a `text` column, and an optional single-dataset `feedback` slot.

Its chart repository is illustrative — this listing demonstrates the definition shape rather than a deployable product.
