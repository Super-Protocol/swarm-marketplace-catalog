apiVersion: v1
kind: Service
metadata:
  name: {{ include "confidential-router-api.fullname" . }}
  labels:
    {{- include "confidential-router-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "confidential-router-api.selectorLabels" . | nindent 4 }}
