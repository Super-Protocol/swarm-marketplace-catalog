apiVersion: v1
kind: Service
metadata:
  name: {{ include "confidential-router-litellm.fullname" . }}
  labels:
    {{- include "confidential-router-litellm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "confidential-router-litellm.selectorLabels" . | nindent 4 }}
