# The console. Nothing here names the image: the origin it calls is an
# environment variable now, so one published digest renders for any pair of
# hostnames — which is what the check in run.sh asserts.
consoleHostname: console.confidential-router.example
apiHostname: api.confidential-router.example
