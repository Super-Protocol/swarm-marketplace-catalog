apiVersion: v2
name: retail-ai-identity
description: Keycloak for the Retail AI agent, with the realm bootstrap that creates its clients, demo user and tenant organization.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: keycloak
    version: 24.4.10
    repository: "file://../vendor/keycloak"
