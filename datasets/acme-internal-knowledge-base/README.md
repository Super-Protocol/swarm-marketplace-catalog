# Acme Internal Knowledge Base

Engineering runbooks, product documentation and support macros, as a ClickHouse table with a free-text `text` column. Restricted by default: it exists to demonstrate a dataset that is compatible with an application *and* has to be granted before it can be bound.
